version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ML_LOG_LEVEL=INFO
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
